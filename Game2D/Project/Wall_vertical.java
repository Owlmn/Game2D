import greenfoot.*;  

public class Wall_vertical extends Actor {  // Наследуем Actor
    public Wall_vertical(int startX, int startY) {
        //GreenfootImage image = new GreenfootImage("wall.png");

        /// Удаляем белый фон (делаем прозрачным)
        //for (int x = 0; x < image.getWidth(); x++) {
        //    for (int y = 0; y < image.getHeight(); y++) {
        //        if (image.getColorAt(x, y).equals(Color.WHITE)) {
        //            image.setTransparency(0);
        //        }
        //    }
        //}

        //setImage(image);
    }
}
